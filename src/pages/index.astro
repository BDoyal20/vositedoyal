---
const year = new Date().getFullYear();
---

<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Bailey Doyal | Voice Actor</title>
    <meta
      name="description"
      content="Voice acting portfolio for Bailey Doyal. Character, commercial, narration, and video game demos with contact info."
    />
    <style>
      :root {
        --bg1: #0b0b10;
        --bg2: #121218;
        --text: #e6e6e9;
        --muted: #a0a0aa;
        --brand: #6366f1;
        --card: #1a1a22;
        --line: #2a2a33;
      }
      * { box-sizing: border-box; }
      html, body { margin: 0; padding: 0; }
      body {
        font-family: ui-sans-serif, system-ui, -apple-system, Segoe UI, Roboto, Helvetica, Arial;
        color: var(--text);
        background: linear-gradient(to bottom, var(--bg1), var(--bg2));
      }
      .wrap { max-width: 1024px; margin: 0 auto; padding: 0 1rem; }
      header { text-align: center; padding: 4rem 0 2rem; }
      h1 { font-size: 3rem; margin: 0; letter-spacing: 0.5px; }
      .tag { color: var(--muted); font-size: 1.125rem; margin-top: 0.5rem; }

      /* Headshot */
      .headshot-container {
        display: flex;
        justify-content: center;
        margin-top: 2rem;
        margin-bottom: 3rem;
      }
      .headshot {
        width: 180px;
        height: 180px;
        border-radius: 50%;
        border: 3px solid var(--brand);
        object-fit: cover;
        box-shadow: 0 0 15px rgba(99, 102, 241, 0.3);
      }

      section { margin: 3rem 0; }
      .section-title {
        font-size: 1.75rem;
        font-weight: 700;
        padding-bottom: 0.5rem;
        border-bottom: 1px solid var(--line);
        margin-bottom: 1.25rem;
      }

      .card {
        background: var(--card);
        border: 1px solid var(--line);
        border-radius: 14px;
        padding: 1rem;
      }
      .grid {
        display: grid;
        grid-template-columns: 1fr;
        gap: 1rem;
      }
      @media (min-width: 720px) {
        .grid { grid-template-columns: 1fr 1fr; }
      }
      h3 { margin: 0 0 0.5rem 0; font-size: 1.125rem; }

      audio { width: 100%; height: 40px; }
      .btn {
        display: inline-block;
        padding: 0.8rem 1.2rem;
        border-radius: 10px;
        background: var(--brand);
        color: white;
        text-decoration: none;
        font-weight: 600;
        transition: filter 0.2s ease;
      }
      .btn:hover { filter: brightness(1.05); }
      .stack { display: grid; gap: 0.75rem; }
      .muted { color: var(--muted); }

      footer {
        border-top: 1px solid var(--line);
        margin-top: 3rem;
        padding: 1.5rem 0 3rem;
        text-align: center;
        color: var(--muted);
      }

      .list {
        display: grid;
        grid-template-columns: 1fr;
        gap: 1rem;
      }
      @media (min-width: 720px) {
        .list { grid-template-columns: 1fr 1fr; }
      }
      .li { padding: 0.75rem 1rem; border: 1px solid var(--line); border-radius: 12px; background: transparent; }

      /* Social links */
      .socials {
        display: flex;
        justify-content: center;
        gap: 1.5rem;
        margin-top: 1rem;
      }
      .socials a {
        color: var(--muted);
        text-decoration: none;
        font-size: 1.2rem;
        transition: color 0.3s ease;
      }
      .socials a:hover {
        color: var(--brand);
      }

      .email {
        text-align: center;
        margin-top: 1rem;
        font-size: 1rem;
        color: var(--muted);
      }
    </style>
  </head>

  <body>
    <header class="wrap">
      <h1>Bailey Doyal</h1>
      <div class="tag">Voice Actor, Narrator, Commercial</div>
    </header>

    <!-- Headshot -->
    <div class="headshot-container">
      <img
        src="/images/headshot.jpg"
        alt="Bailey Doyal Headshot"
        class="headshot"
      />
    </div>

    <main class="wrap">
      <!-- Demo Reels -->
      <section>
        <div class="section-title">Demo Reels</div>
        <div class="grid">
          <div class="card">
            <h3>Character Demo</h3>
            <audio controls preload="metadata">
              <source src="/audio/character-demo.mp3" type="audio/mpeg" />
            </audio>
            <p class="muted" style="margin-top:0.5rem">Dynamic characters, anime, and games.</p>
          </div>
          <div class="card">
            <h3>Commercial Demo</h3>
            <audio controls preload="metadata">
              <source src="/audio/commercial-demo.mp3" type="audio/mpeg" />
            </audio>
            <p class="muted" style="margin-top:0.5rem">Conversational, trustworthy, and upbeat reads.</p>
          </div>
          <div class="card">
            <h3>Narration Demo</h3>
            <audio controls preload="metadata">
              <source src="/audio/narration-demo.mp3" type="audio/mpeg" />
            </audio>
            <p class="muted" style="margin-top:0.5rem">Documentaries, audiobooks, and corporate narration.</p>
          </div>
          <div class="card">
            <h3>Video Game Demo</h3>
            <audio controls preload="metadata">
              <source src="/audio/videogame-demo.mp3" type="audio/mpeg" />
            </audio>
            <p class="muted" style="margin-top:0.5rem">Energetic, emotional, and immersive performances.</p>
          </div>
        </div>
      </section>

      <!-- About -->
      <section>
        <div class="section-title">About</div>
        <div class="card stack">
          <p>
            Versatile voice with clear articulation and grounded delivery. Experienced with character acting, commercial reads, and narration. Fast turnaround with clean, broadcast-ready audio.
          </p>
          <div class="list">
            <div class="li">
              <strong>Range</strong><br />
              Warm mid-low, youthful to authoritative
            </div>
            <div class="li">
              <strong>Accents</strong><br />
              General American, light Southern, neutral
            </div>
          </div>
        </div>
      </section>

      <!-- Studio -->
      <section>
        <div class="section-title">Studio Specs</div>
        <div class="card list">
          <div class="li"><strong>Mic</strong><br />UA Sphere LX or equivalent LDC</div>
          <div class="li"><strong>Interface</strong><br />Apollo Twin X or comparable</div>
          <div class="li"><strong>Chain</strong><br />Clean, minimal noise floor</div>
          <div class="li"><strong>Delivery</strong><br />WAV 48 kHz 24-bit, or per spec</div>
        </div>
      </section>

      <!-- Contact -->
      <section id="contact">
        <div class="section-title">Contact</div>
        <div class="card stack" style="text-align:center;">
          <p class="muted">Project in mind or want a custom audition?</p>

          <div class="email">
            📧 <a href="mailto:bailey.doyal0@gmail.com" style="color:var(--text);text-decoration:none;">bailey.doyal0@gmail.com</a>
          </div>

          <div>
            <a class="btn" href="mailto:bailey.doyal0@gmail.com">Email Me</a>
          </div>

          <div class="socials">
            <a href="https://twitter.com/" target="_blank" rel="noopener noreferrer">Twitter</a>
            <a href="https://facebook.com/" target="_blank" rel="noopener noreferrer">Facebook</a>
            <a href="#" target="_blank" rel="noopener noreferrer">More…</a>
          </div>

          <p class="muted" style="margin-top:0.75rem;">Response within one business day.</p>
        </div>
      </section>
    </main>

    <footer>
      © {year} Bailey Doyal • Voice Acting Portfolio
    </footer>
  </body>
</html>
