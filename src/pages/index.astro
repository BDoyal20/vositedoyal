import React from "react";
import { render } from "astro/runtime";

export default function VoiceActingPortfolio() {
  return (
    <main className="min-h-screen bg-gradient-to-b from-gray-900 to-black text-gray-100 font-sans">
      <header className="text-center py-16">
        <h1 className="text-5xl font-bold tracking-tight">Bailey Doyal</h1>
        <p className="text-xl mt-4 text-gray-400">Voice Actor | Narrator | Storyteller</p>
      </header>

      <section className="max-w-4xl mx-auto px-6 mb-24">
        <h2 className="text-3xl font-semibold mb-6 border-b border-gray-700 pb-2">
          Demo Reels
        </h2>
        <div className="space-y-8">
          <div>
            <h3 className="text-xl font-medium mb-2">Character Demo</h3>
            <audio controls className="w-full rounded">
              <source src="/audio/character-demo.mp3" type="audio/mpeg" />
              Your browser does not support the audio element.
            </audio>
          </div>
          <div>
            <h3 className="text-xl font-medium mb-2">Commercial Demo</h3>
            <audio controls className="w-full rounded">
              <source src="/audio/commercial-demo.mp3" type="audio/mpeg" />
              Your browser does not support the audio element.
            </audio>
          </div>
        </div>
      </section>

      <section className="max-w-4xl mx-auto px-6 mb-24">
        <h2 className="text-3xl font-semibold mb-6 border-b border-gray-700 pb-2">
          About Me
        </h2>
        <p className="text-lg leading-relaxed text-gray-300">
          I’m a passionate voice actor with experience in character performance, narration, and
          commercial reads. My voice has been described as versatile, grounded, and expressive.
          I bring emotion and precision to every project, ensuring clear and professional results.
        </p>
      </section>

      <section className="max-w-4xl mx-auto px-6 mb-24">
        <h2 className="text-3xl font-semibold mb-6 border-b border-gray-700 pb-2">
          Contact
        </h2>
        <p className="text-lg text-gray-300 mb-4">
          Ready to collaborate or want to request a custom audition?
        </p>
        <a
          href="mailto:yourname@email.com"
          className="inline-block bg-indigo-600 hover:bg-indigo-700 text-white font-medium px-6 py-3 rounded-lg transition"
        >
          Contact Me
        </a>
      </section>

      <footer className="text-center py-10 border-t border-gray-800 text-gray-500">
        <p>© {new Date().getFullYear()} Bailey Doyal | Voice Acting Portfolio</p>
      </footer>
    </main>
  );
}

render(<VoiceActingPortfolio />);
